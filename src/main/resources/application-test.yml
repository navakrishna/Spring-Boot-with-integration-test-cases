spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:sa
    username: sa
    password: 
  h2: 
    console:
      enabled: true
    path: /h2
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
     
    
valid:
  commands: BALANCE,SEND,TOTAL,SENT